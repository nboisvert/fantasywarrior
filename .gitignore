# .NET
bin/
obj/
*.user
*.suo
.vs/
artifacts/
TestResults/
*.binlog

# React / Vite / Node
node_modules/
dist/
npm-debug.log*
yarn-error.log

# Environment & secrets
# This repository is PUBLIC. Anything committed here is readable by anyone,
# forever, including from git history — so real credentials only ever live in
# appsettings.Local.json, never in the committed appsettings.json.
appsettings.Local.json
appsettings.*.Local.json
.env
*.local

# IDE
.idea/
*.swp

# OS
Thumbs.db
.DS_Store

# Firebase emulator
firebase-debug.log
firestore-debug.log

# Claude Code agent worktrees
.claude/worktrees/
